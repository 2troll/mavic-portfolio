# Fotos de las reseñas aprobadas

No toques esta carpeta a mano: la llena `resenas.py publicar` desde el repo
`tony-kansai-guide`. Cada foto se guarda como `<id>.jpg`, con el mismo id que
en `public/reviews.json`.
